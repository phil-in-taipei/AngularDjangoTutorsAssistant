import { AddMinutePipe } from './add-minute.pipe';

describe('AddMinutePipe', () => {
  it('create an instance', () => {
    const pipe = new AddMinutePipe();
    expect(pipe).toBeTruthy();
  });
});
